# ABOUTME: Round-7 fresh test review of PR #18 at d123a41 (test-reviewer, worktree-isolated, Swift allowed)
# ABOUTME: M1-M4 closure verdicts, M2 trigger mutation sweep, double parity probe, new findings, could-not-verify list

## Test Review — PR #18 test suite at d123a41023acc7002a05a4002510f17a2551e17b

Isolation: launch carried `isolation: "worktree"`, brief names the base SHA, `git rev-parse --git-dir` (`.git/worktrees/agent-ad958f73645823682`) differs from `--git-common-dir` (`.git`). Writes were confined to `export-d123a41/` inside the worktree (materialized with `git archive d123a41 | tar -x`). Two `diff -rq` checks against a fresh `git archive d123a41` (before the sweep and after the last restore) reported zero differing files; every mutant was restored byte-for-byte (SHA-256 `bdfe12bb…3168b` before and after). No shared git state touched, no network, no database outside temp dirs the tests create.

Environment for every Swift run: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`, `JIDOKA_RELEASE_RUNTIME_ROOT=/private/tmp/jidoka-code-progressive-production-automation/build/runtime-input/qualified-runtime`, one `swift build`/`swift test` at a time, all inside `export-d123a41`.

### Scope actually covered
- M1: `RolloutAuthorityStoreTests.readbackDuringDrain` (:3395-3483), `gitReadbackDuringDrain` (:3306-3393), `stopDrainOrderingIsDurable` (:1844-1884), `transitionLane` delta (RolloutAuthorityStore.swift:2075-2089), readback triggers (DatabaseSchema.swift:3288-3327, twin :3364-3403), lane-close guard (:2696-2707), `RolloutLeaseAuthorityTests.bothTriggersShareTheSameGate` (:391-420).
- M2: `usageInsertCannotForgeBudget` (:1015-1058), `reservationsConsumeCap` (:915-1013), trigger `rollout_authorization_usage_exact_insert` (DatabaseSchema.swift:2715-2829), 11-mutant sweep (below).
- M3: `RolloutEffectAuthorityTestSupport.swift` (all 698 lines) vs `RolloutEffectAuthorityStore.swift` (:1-120, :195-324, :399-735, :796-875) and `RolloutEffectAuthorityStoreTransactions.swift` (:630-690, :973-1112); `HistoricalCanaryEffectDenialTests.swift` (all); `HerdrPiWorkflowExecutor.swift` :4154-4260, :4912-4990 plus the round-6 hunks; `HerdrPiWorkflowRuntimeTests.swift` retained tests :16-32, :376-731, :1404-1628, :1822-2276, :2278-2428, fixture `make` :3962-4109, `withDurableResumeDenialLifted` :4347-4396, compared function-by-function against `git show 4206456:` (old :374-455, :2783-3037, outline of all 42 old tests); `PullRequestReviewJobWorkflowTests.topologyRecoveryReviewSelection` hunk.
- M4: `RolloutCLITests.swift` (all), `RolloutOperatorFixture.swift` (all), `ViewModelFlowTests.rolloutPreviewInputGate` hunk plus `DelayedSnapshotEngineClient`, `EngineProtocolTests` hunk, `ProductionRolloutOperatorTests` (RolloutAuthorityStoreTests.swift:3016-3302), `ProductionEngineJobRuntime.swift` round-6 hunks (seams).
- Suite architecture: `Package.swift`, `scripts/tests/test-progressive-production-automation.sh`, Makefile target, greps for `withKnownIssue`, `try?`, sleeps, real clocks, `settleGitRemoteRead` coverage, `ApprovedCommandRunStoreTests` and `SQLiteStoreTests` hunks.

### Probes run (exact commands, outcomes)

1. Build: `swift build --build-tests` in `export-d123a41` → `Build complete! (32.70s)`, exit 0.
2. Baseline: `swift test --filter 'RolloutAuthorityStoreTests'` → `Test run with 45 tests in 2 suites passed after 1.268 seconds` (includes `usageInsertCannotForgeBudget`, confirmed started/passed in the log).
3. M2 mutation sweep (`tr-m2-mutants.sh`, logs in `SCRATCH/round7/tr-m2-mutants/`): each mutant = `sed` edit of `Sources/JidokaCodeCore/State/DatabaseSchema.swift` inside the export (diff saved as `<name>.diff`), `swift build --build-tests` (exit 0 confirmed for all), `swift test --skip-build --filter 'RolloutAuthorityStoreTests'` (45 tests executed each time), file restored from the saved original. `SQLiteStoreTests` (pinned digest) deliberately excluded per brief.

| Mutant | Edit (line at d123a41) | Result |
|---|---|---|
| M1_contiguity | delete :2810 `AND NEW.sequence = previous.sequence + 1` | SURVIVED, 45/45 pass |
| M2_provider_counter | delete :2817 provider_sessions equality | SURVIVED |
| M3_source_id | :2811 drop `AND NEW.source_id = source.source_id` | SURVIVED |
| M4_source_kind | :2811 drop `NEW.source_kind = source.source_kind` | SURVIVED |
| M5_timestamp | delete :2812 `AND NEW.created_at_ms = source.created_at_ms` | SURVIVED |
| M6_seed_provider_zero | delete :2725 `AND NEW.provider_sessions = 0` (seed branch) | SURVIVED |
| M7_seed_activated_at | :2735 `activated_at_ms = NEW.created_at_ms` → `1 = 1` | SURVIVED |
| M8_previous_is_latest | delete :2806-2809 (`previous.sequence = (SELECT MAX…)`) | SURVIVED |
| M9_read_bytes_counter | delete :2815 github_read_bytes equality | SURVIVED |
| M10_seed_source_kind | :2719 `NEW.source_kind = 'activation'` → `1 = 1` | SURVIVED |
| M11_whole_trigger | delete :2714-2830 (entire statement) | KILLED: `usageInsertCannotForgeBudget` failed with 3 issues (all three forged rows accepted) |

   Restore verified: `restored sha256 … (match=yes)`, `restore build exit=0`.
4. Double parity probe (`ReviewerDoubleParityProbeTests.swift`, copied into `Tests/JidokaCodeCoreTests/`, built, run with `swift test --skip-build --filter 'ReviewerDoubleParityProbeTests'`, then removed; final `swift build --build-tests` exit 0): reserve provider/command/git-send permits with the double under `.workflow(jobID)`, then under `.historicalCanary(jobID)` call verify/bind. Output: `PROBE double granted under historical context: ["verifyProviderPermit", "bindProviderReservation", "bindApprovedCommandReservation", "verifyGitSendPermit"]`. The store denies all four (`RolloutEffectAuthorityStore.swift:474-481`, `:538-545`, `:683-690` require `.workflow` context or throw `effectAdmissionClosed`; `RolloutEffectAuthorityStoreTransactions.swift:1050-1055` throws `effectIdentityMismatch`).

### Closure verdicts

**M1 (readback during drain): CLOSED, one evidence gap (Minor 1).** `transitionLane` now runs the `paused = 1` UPDATE for every target including `.draining` (delta :2075-2089; the `if target != .draining` guard is gone). `readbackDuringDrain` reserves (`.readback` permit), verifies and settles a real GitHub readback against the real store after `beginDrain`, asserts `paused == 1`, and asserts a fresh `reserveProvider` under workflow context throws typed `effectAdmissionClosed`; `gitReadbackDuringDrain` does the same for a `.gitReadback` permit and denies a fresh git send. Both are green (baseline run). `stopDrainOrderingIsDurable` was re-pointed from `paused == 0` to `paused == 1` during drain, which is the intended behavioural flip, not a weakening. The prior review's specific ask (make the trigger-text shared-gate test cover the readback triggers) was not done; the writer says so (Minor 9 disposition). No test drives the SQL lane-close guard (:2696-2707) directly with `state='draining' AND paused=0`; the Swift path makes that state unreachable, so this is under-evidenced, not open.

**M2 (usage INSERT gate): closed in source, UNDER-EVIDENCED at Major level (Major 1).** The trigger exists and rejects the three forged rows (M11 kill proves the test depends on the trigger's existence). But every forged row violates several conjuncts at once (invented `source_id`, wrong counters, wrong sequence), so the ten single-predicate mutants above all survive: no test distinguishes the seed branch, contiguity, latest-previous, per-counter arithmetic, source kind, source ownership or timestamp coherence. The writer's own report says "Full store tests exercise all four honest append paths"; honest paths cannot detect a loosened predicate. The pinned digest test (SQLiteStoreTests) flags any byte change to migration 10 but cannot tell a lost predicate from a legitimate edit, so it is not behavioural evidence.

**M3 (test double grants what the store denies): CLOSED for the reported defect, with one latent parity gap (Minor 2) and a matrix-honesty note (Minor 3).**
- The double now denies historical reserve/send/verify(`.historicalCanary` permit)/settle like the store; `HistoricalCanaryEffectDenialTests` runs each of those parameterized over `[double, store]`.
- The executor's historical provider fallback is deleted (delta :284-352) and a hard guard `if case .historicalCanary = … { throw effectAdmissionClosed }` sits at `HerdrPiWorkflowExecutor.swift:4217` before slot lookup. Note for the record: `HerdrPiRuntimeFixture.make` still wires the double (`rolloutAuthority: ExplicitTestRolloutEffectAuthority()`, HerdrPiWorkflowRuntimeTests.swift:4066), so the retained Herdr tests prove the executor guard, not store behaviour; the store side is covered separately by the denial suite. That is acceptable.
- Retired: ten functions, not nine. Removed from 4206456 → d123a41: `roleHostReplacementAmbiguityIsTerminal`, `…CleanupFailureIsExact`, `…CloseReopenRecovery`, `…CrashRecoveryIsIdempotent`, `…HappyPathIsIsolated`, `…PreCutoverFaultMatrix`, `…PreparedIntentRestartContinuesOriginalReceipt`, `…RevalidatesQ4AtEffectBoundaries`, `…SentIntentRestartIsTerminal`, `…TerminalUnknownCloseReopenIsolation`; renamed/rewritten: `pausedExactCanaryCreatesTopologyAndRun` → `pausedExactCanaryCannotLaunch`, `generationRolloverSettlesOnlyQ4` → `generationRolloverCannotLaunchQ4`; new: `historicalReplacementDenialPreservesEvidence` (42 → 33 `@Test`). `roleHostReplacementHappyPathIsIsolated` is the unlisted tenth; its replacement is the new denial test. Report inaccuracy only.
- Retained denial tests are not tautologies: `pausedExactCanaryCannotLaunch` (:376-444) asserts typed `effectAdmissionClosed` at `executor.execute`, then zero runs, zero role hosts, zero launched commands, zero attributed topology intents, `paused` still 1. `historicalReplacementDenialPreservesEvidence` (:2191-2276) snapshots launches/hosts/job record/commands/remote/replacement operations before, asserts equality after denial, checks no projected credential and no command file, then repeats after reopen. `explicitUnknownTopologyRecovery` (:718-728) asserts denial plus unchanged hosts/commands. `PullRequestReviewJobWorkflowTests.topologyRecoveryReviewSelection` now asserts denial with job record, intents, comments and artifacts unchanged.
- No retained assertion was weakened to go green: the only removed `#expect` lines in Tests outside the Herdr file are the M1 `paused == 0 → 1` flips, the PR-review test's `succeeded`/`createCommentCount == 1` replaced by stronger preservation assertions, the `(any Error).self → SQLiteStoreError.self` tightening, and `migrations.count == 10 → >= 10`.
- `generationRolloverCannotLaunchQ4` expects `effectIdentityMismatch` (not `effectAdmissionClosed`) at :1610; that is the executor's `verifyProviderLaunchAuthority` :4929 path (no permit), consistent with source, but the reason differs from the other denial tests and is not explained in the test.
- "51 post-preview scenarios prove denial": honest as worded, but 49 of the 51 parameterized cases (all but `.replacementEvidence`/`.replacementQ4Authorization`) now inject a distinct drift and then hit the same admission guard before any drift is examined; the per-case `expectedError` is dead for those 49 and each case pays a full fixture (`prepareReplacementIncident`) to make one identical assertion. The 28-entry `replacementPreCutoverFaultCases` (:3153-3300, with `ReplacementPreCutoverFaultKind`, `perform`) is now referenced only by the shape count in `runtimeMatrixShape` :24.

**M4 (operator path untested): CLOSED.** `RolloutCLITests` covers every action's arity (+1/-1 argument), mode swaps, non-canonical UUID, bad UUID, `{}`, trailing-byte non-canonical JSON, NaN timeout, 999/660001 bounds, short digests, digest mismatches, strict base64 (`""`, `"_"`, `"AR=="`, newline, 1 MiB+1, oversized string), and the 700 s budget for stop/recovery vs 30 s otherwise. `ProductionRolloutOperatorTests` runs the real `ProductionEngineJobRuntime` against a real `RolloutAuthorityStore` with real `EngineDispatchGate`/`ApprovedCommandExecutionGate` and asserts: `.busy` when unpaused / exclusive / checkpointing; `paused` flag and scheduler `[true, false]` on activation; on stop, dispatch gate false, command gate `launchSuppressed`, scheduler paused, `paused=1`, `.staleEvidence` on wrong digest and on repeat; drain timeout → `recoveryRequired` with `DRAIN_TIMEOUT`, preview does not mutate status, execute recovery leaves dispatch closed under exclusivity; recovery `.staleEvidence` when unpaused, when expired (601 s), and when evidence drifted; configuration drift → `failed`/`CONFIGURATION_DRIFT` with gates closed. `ViewModelFlowTests.rolloutPreviewInputGate` covers paused gating, the same base64 boundary list, an expired-input JSON, and `isWorking` busy state. Seams: `EngineDispatchGate` went `private` → internal and the second `ProductionEngineJobRuntime.init` (internal, :203) gained `dispatchGate:`/`commandGate:` defaults; `ProductionEngineReloadComposition.schedulerSnapshot` added. None is `public`; reachable only via `@testable`. The public init (:172) constructs its own gates. Acceptable.

### CRITICAL
None.

### MAJOR
- **Major 1. Tests/JidokaCodeCoreTests/RolloutAuthorityStoreTests.swift:1015-1058 (`usageInsertCannotForgeBudget`) vs Sources/JidokaCodeCore/State/DatabaseSchema.swift:2715-2829.** Claim: the M2 trigger's individual predicates have no behavioural coverage; the only negative test rejects rows that are wrong in several ways at once, so any single conjunct can be deleted without a red test. Fix: add single-fault forgeries built from a *valid* next row (copy the honest sequence-5 row that `reserveEffect` would produce, then perturb exactly one field per case): (a) sequence = previous + 2 with everything else exact (contiguity), (b) previous = sequence 3 instead of latest (latest-previous), (c) source_id of an already-consumed reservation with exact counters (ownership), (d) source_kind `scopeRead` with an effect-reservation id (kind), (e) created_at_ms +1 (timestamp), (f) one counter off by one for each of the twelve, (g) a second seed row (sequence 0, `activation`) after activation, (h) seed row with a nonzero counter, (i) seed row whose created_at_ms differs from `activated_at_ms`. Each must throw `SQLiteStoreError` and leave `COUNT(*)` unchanged. Evidence: 10/10 single-predicate mutants survive `swift test --filter RolloutAuthorityStoreTests` (45/45 green each, build confirmed), whole-trigger deletion killed (3 issues), diffs and logs under `SCRATCH/round7/tr-m2-mutants/`. Rubric: missing test coverage for new code (defence-in-depth trigger that is the M2 fix itself). Not a source defect: the trigger text at d123a41 is correct on reading.

### MINOR
- **Minor 1. Sources/JidokaCodeCore/State/DatabaseSchema.swift:2696-2707 and :3288-3327/:3364-3403.** Claim: the M1 SQL side (lane-close guard requiring pause while draining; readback trigger's `draining AND paused = 1` disjunct) is proven only through the Swift path, which now always pauses; no test inserts a readback row or transitions a lane with `paused = 0` at the SQL level, and `bothTriggersShareTheSameGate` still covers only the two lease triggers although the readback disjunction is hand-duplicated (:3311-3317 vs :3387-3393). Fix: a test that (1) tries the lane transition with `paused = 0` and expects `statementFailed`, and (2) asserts the two readback `exact_started_effect` WHEN bodies are byte-identical after the table-name substitution. Evidence: the round-6 delta removes the exemption `AND NOT (OLD.state = 'active' AND NEW.state = 'draining')` from the guard's WHEN clause (delta hunk `@@ -2692,7 +2697,6 @@`), so an unpaused active→draining transition is now a SQL abort, yet `grep -rn "open_lane_close_requires_pause\|state = 'draining'" Tests` is empty; the shared-gate test names only `repository_leases_rollout_authority_{insert,update}` (:414-415).
- **Minor 2. Tests/JidokaCodeCoreTests/RolloutEffectAuthorityTestSupport.swift:104-139, :166-176, :259-264.** Claim: the double still grants `verifyProviderPermit`, `bindProviderReservation`, `bindApprovedCommandReservation` and `verifyGitSendPermit` on an ordinary `.reservation` permit while the task context is `.historicalCanary`, where the store throws (`RolloutEffectAuthorityStore.swift:474-481`, `:538-545`, `:683-690`; `…Transactions.swift:1050-1055`). No test currently depends on it (`grep -rn historicalCanary Tests` hits only the denial suite and the double), so it is latent, same class as the prior Minor 23, but it contradicts the round-6 claim "store/double parity is parameterized across reserve, send, verify and settle": the verify/bind parity cases only use a `.historicalCanary` permit. Fix: call `denyHistoricalContext()` (or require `.workflow` context) in those four double methods and extend `historicalCanaryCannotVerifyOrSettle` with a workflow-issued reservation verified under historical context for both authorities. Evidence: reviewer probe output above (run and removed; no residue).
- **Minor 3. Tests/JidokaCodeCoreTests/HerdrPiWorkflowRuntimeTests.swift:1933-2069 and :3153-3300.** Claim: 49 of the 51 `roleHostReplacementRejectsEveryPostPreviewDrift` cases execute an identical path (drift injected, then denied at admission :2023) at full-fixture cost, with `expectedError` dead for those cases; the 28-case `replacementPreCutoverFaultCases` catalogue plus `ReplacementPreCutoverFaultKind`/`perform` machinery is referenced only by a count assertion (:24-31). Fix: collapse the 49 into one denial test (keep the two authorization-drift cases as their own parameterized test) and delete the fault catalogue or move it to the retired-test note; the writer's comment (:22-23) already concedes it is fixture data. Evidence: `grep -n expectedError` shows uses only at :1983/:1998 and inside the catalogue definitions.
- **Minor 4. Sources/JidokaCodeCore/Git/GitTransport.swift:578-589.** Claim: the round-6 change to `try? settleGitRemoteRead` on the failure path (Minor 2 disposition) has no test: nothing in Tests references `settleGitRemoteRead`, and `GitRolloutPreviewTests.readAdmissionClosesBeforeTransport` (:92-129) fails before the body runs, so neither "primary error is preserved when settlement throws" nor "reservation stays consumed" is asserted. Fix: a transport test with a runner that throws after verify, and a double whose `settleGitRemoteRead` throws, asserting the runner's error surfaces and `waitForDrain` stays false. Evidence: `grep -rn settleGitRemoteRead Tests` → no matches.
- **Minor 5. quality_reports (round-6 report, "Historical coverage correction").** Claim: the report lists nine retired functions; the delta removes ten (`roleHostReplacementHappyPathIsIsolated` unlisted, effectively replaced by `historicalReplacementDenialPreservesEvidence`). Fix: correct the list. Evidence: function-name diff of `git show 4206456:` vs d123a41 (42 vs 33 `@Test`, saved as `SCRATCH/round7/tr-herdr-{old,new}.txt`).
- **Minor 6. Tests/JidokaCodeCoreTests/RolloutAuthorityStoreTests.swift:3089-3146 (`drainTimeoutAndRecovery`).** Claim: real-clock race: a 1 s drain timeout runs on `ContinuousClock` while the test polls the dispatch gate up to 2 s and then asserts `state == 'draining'` (:3113-3115); if the runner is descheduled for >1 s between the gate closing and that read, the lane is already `recoveryRequired` and the assertion fails. Low probability, bounded; note only. Fix: assert `draining` from inside the polling loop or raise the timeout to 5 s. Evidence: :3103 `timeoutMilliseconds: 1_000`, :3107 `advanced(by: .seconds(2))`.
- **Minor 7. Tests/JidokaCodeCoreTests/HerdrPiWorkflowRuntimeTests.swift:1610 (`generationRolloverCannotLaunchQ4`).** Claim: the expected error is `effectIdentityMismatch` while every sibling denial test expects `effectAdmissionClosed`; it passes because `verifyProviderLaunchAuthority` (:4912-4930) is reached with a nil permit, but nothing in the test says which boundary denies. Fix: a comment naming the boundary, or route the Q4 launch through the same `.historicalCanary` guard for a uniform error. Evidence: executor :4927-4929.

### Missing Test Cases
1. Single-fault usage-ledger forgeries (a)-(i) in Major 1.
2. SQL-level `state='draining'`/`paused=0` refusal and readback-trigger twin equality (Minor 1).
3. Workflow-issued reservation verified/bound under historical context, double and store (Minor 2).
4. Git remote read: transport failure with throwing settlement keeps the primary error and the reservation consumed (Minor 4).

### Suite architecture and honesty (checked, no finding)
- `Package.swift`: `JidokaCodeTestSupport` is a plain `.target` at `Tests/JidokaCodeTestSupport` depended on only by the three test targets; no product exports it; production targets do not depend on it.
- Acceptance script lists 21 suites, all present (`ProductionRolloutOperatorTests` is `@Suite("Production rollout operator commands")` at RolloutAuthorityStoreTests.swift:3016). It still checks suite names and a positive summary only; it does not invoke `--rollout` end to end (the CLI parser is now unit-tested, which is what M4 asked for).
- `withKnownIssue`: zero occurrences in Tests. `try?` in the rollout suites: cleanup only (RolloutAuthorityStoreTests.swift:2912) and one absence assertion (HerdrPiWorkflowRuntimeTests.swift:2057).
- 700 s appears once in `RolloutCLI.responseTimeoutSeconds` and once in `ApplicationEngineClient.swift:89`; `RolloutCLITests.responseBudgets` pins both kinds against every `EngineCommandKind`.
- Error assertions in the reviewed tests are typed (`RolloutAuthorityError.<case>`, `EngineClientError(.<code>)`, `SQLiteStoreError.self` where the raw abort is the contract); no string matching on error text.
- Round-6 test tightenings verified (`SQLiteStoreError.self`, `defer` in loop bodies, `.effectIdentityMismatch` for historical git send).
- Test-only seam `liftDurableResumeDenial` (HerdrPiWorkflowRuntimeTests.swift:4374-4396) drops and restores two `app_settings` triggers in the test database only; restore failure is recorded as an issue; no production seam.

### Could not verify
- Full `make check` / `make test-e2e` were not re-run (coordinator holds fresh logs); only the filtered runs above were executed.
- Decision-14 entry-path and decision-15 effect-boundary coverage matrices were not re-audited row by row; the prior review found no gap there and nothing in the round-6 delta touches those paths except the M3 executor guard.
- Whether the historical role-host replacement machinery in the executor is now dead production code is an architecture question (arch reviewer), not assessed here beyond noting that its tests reach `activateCanaryRoleHostReplacement` successfully and are denied at execution.

### Summary
Coverage assessment: M1, M3, M4 closed with real assertions at the right boundaries and typed errors; M2 is fixed in source but its only negative test cannot see any single predicate of the trigger (10/10 mutants survive). Suite is 770/88 per writer logs; the filtered runs here were green on the unmodified export.
Recommendation: FIX BEFORE MERGE (one Major: single-fault usage-ledger tests; seven Minors).
