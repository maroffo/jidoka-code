# ABOUTME: Round-7 fresh architecture review of PR #18 at d123a41 (pause/drain, canary seam, runtime, fail-closed, error typing)
# ABOUTME: Findings in Finding Contract shape; evidence by source citation, SQL trigger reading and proposed probes

# Architecture Review — PR #18 at d123a41 (round 7, fresh, from zero)

Status: COMPLETE.

## Scope and method
- Launched with `isolation: "worktree"`, base SHA d123a41023acc7002a05a4002510f17a2551e17b. The copy's HEAD is `main`; all citations are against `SCRATCH/round7/export/` (byte-identical to d123a41 for Sources/Tests/docs).
- No `swift build`/`swift test` (forbidden for this reviewer). Evidence: source citation, SQL trigger reading, the round-6 delta patch, and proposed red-green probes for the coordinator. No git state mutated; nothing written outside `SCRATCH/round7/reports/`.
- Files read (Sources, d123a41): `State/RolloutAuthorityStore.swift` (88-140 activate, 275-343 activeStatus, 361-380 status/latestStatus, 550-749 beginDrain/markRecoveryRequired/resumeExactRecovery/revoke/settle/fail/markInterruptedLaneRecoveryRequired, 1871-1890, 2040-2113 transitionLane), `State/RolloutAuthority.swift` (40-58, 1588-1695), `State/RolloutEffectAuthorityStore.swift` (1-300, 399-560, 796-870), `State/RolloutEffectAuthorityStoreTransactions.swift` (24-215, 320-645, 1247-1362), `State/DatabaseSchema.swift` (2636-2715, 2842-2930 headers, 3244-3300, 3369-3520, 3582-3650), `Application/ProductionEngineJobRuntime.swift` (133-250, 290-700, 708-830, 1283-1395, 1640-1700), `Application/EngineService.swift` (340-440, 555-665, 1268-1295), `Scheduler/DurableScheduler.swift` (370-440) + `SchedulerTimingState.takeDuePass`, `Jobs/JobCoordinator.swift` (40-130, 250-520, 1055-1140, failJob, classifier), `Jobs/PullRequestReviewJobWorkflow.swift` (357-520), `Jobs/PiJobWorkflowPreparer.swift` (40-150), `Pi/HerdrPiWorkflowExecutor.swift` (331-339, 1805-1807, 4154-4300, 4700-4770, 4840-4960, 5140-5215, 5660-7036 call sites), `Reconciliation/MutationReconciler.swift` (583-602), `Git/GitPublication.swift` (183-197), `GitHub/GitHubBroker.swift` (72-112, 404-476), `Git/GitTransport.swift` (150-162, 232-262, 548-595). Tests consulted for closure evidence only: `RolloutAuthorityStoreTests.swift` (test titles, 3306-3395), `JobCoordinatorTests.swift` (114-160), `RolloutEffectAuthorityTestSupport.swift` (grep), `EngineProtocolTests.swift` (titles). Plan: locked decisions 4, 14, 15, 20, 21, 22, 30 and E1-E19. Runbook: `docs/operations/progressive-production-rollout.md` 308-326.

## Commands run (exact, all read-only in the export)
```
grep -rn "historicalCanary\|requireHistoricalCanary\|JobCanaryScope" Sources
grep -rn "RolloutEffectAuthorizing\b" Sources
grep -n "CREATE TRIGGER" Sources/JidokaCodeCore/State/DatabaseSchema.swift
grep -n "transitionLane(" -B 12 -A 12 Sources/JidokaCodeCore/State/RolloutAuthorityStore.swift
grep -n "canaryJobID\|verifyProviderLaunchAuthority(\|activeCanaryPiFreshRetry|..." Sources/JidokaCodeCore/Pi/HerdrPiWorkflowExecutor.swift
grep -n "enqueuePreparedLaunch(\|ensureCommandPublished(" -A 6 Sources/JidokaCodeCore/Pi/HerdrPiWorkflowExecutor.swift
grep -rn "mode: \.readback\|\.readback(jobID" Sources          # producers of the readback context
grep -rn "RolloutEffectTaskContext\|mode: \.\(workflow\|readback\|historicalCanary\|discovery\)" Sources/JidokaCodeCore/Jobs/*.swift
grep -rn "statementFailed\|SQLiteStoreError" Sources | grep -v State/SQLiteStore.swift   # -> no matches
grep -rn "rolloutAuthority: nil" Tests                          # 4 hits, 2 files
grep -n "@Test" Tests/JidokaCodeCoreTests/RolloutAuthorityStoreTests.swift | grep -i "drain\|readback\|stop\|recover"
```
Output excerpts that settle claims are quoted inline under each finding.

## Verdict on M1-M4 (independent)

### M1 (readback during drain): CLOSED
- `transitionLane` (RolloutAuthorityStore.swift:2074-2091) now writes `paused = 1` on every lane transition and clears `active_rollout_authorization_id` only when `!target.isOpenLane` (`isOpenLane` = active|draining|recoveryRequired, RolloutAuthority.swift:49-51). `active -> draining` therefore retains the id and pauses in the same transaction. Delta hunk confirms the former `if target != .draining` skip was removed.
- SQL guard `rollout_authorizations_open_lane_close_requires_pause` (DatabaseSchema.swift:2696-2707): the `NOT (OLD.state='active' AND NEW.state='draining')` exemption is gone; every open-lane transition now requires pause first, matching the Swift write order (paused UPDATE precedes the state UPDATE inside one `database.transaction`).
- Readback insert triggers (`rollout_readback_reservations_exact_started_effect` 3405-3445; git twin 3481-3518) admit `(active AND paused=0 AND id=auth) OR (draining|recoveryRequired|settled|revoked|expired|failed AND paused=1)`. `waitForDrain` (RolloutEffectAuthorityStore.swift:831-870) counts reserved readbacks under open lanes as in-flight, so `stopAndDrainRollout`'s loop (ProductionEngineJobRuntime.swift:511-527) waits for them before `revoke`. Coherent.
- State reachability table (Swift producers vs SQL admission):
  | app_settings / lane | Producer in Swift | SQL readback | SQL fresh effect | Swift fresh effect |
  |---|---|---|---|---|
  | active, paused=0, id=auth | `activate`, `resumeExactRecovery` (RolloutAuthorityStore.swift:661-671) | admit | admit | admit (`loadActiveBinding` Transactions.swift:1282-1286) |
  | draining/recoveryRequired, paused=1, id=auth | `transitionLane` | admit | refuse | typed `effectAdmissionClosed` (`loadActiveBinding`) |
  | terminal, paused=1, id=NULL | `transitionLane` | admit iff source effect still sendStarted/observationRequired | refuse | typed `effectAdmissionClosed` |
  | active, paused=1 | none: `.setPaused(true)` is refused while the lane is open (EngineService.swift:581), `transitionLane` never targets `active` | refuse (RAISE) | refuse | typed |
  No Swift-reachable state is admitted by SQL in the wrong direction; the only SQL-refused-but-Swift-unguarded cell is the unreachable `active,paused=1` row (see Minor 3 for the error-typing consequence). Decision 20 (post-stop readback of started effects) and 22 as amended by E16 are honored; the runbook (docs 310-311) states the amended order.
- Evidence weakness (not a defect): the two drain tests (`RolloutAuthorityStoreTests.swift:3306`, `:3395`) bind `.readback` at the store directly (line 3371-3374 of the file: `RolloutEffectTaskContext.$current.withValue(RolloutEffectExecutionContext(mode: .readback(...)))`). The production producers bind the identical context at the reader boundary (`MutationReconciler.swift:593-596`, `GitPublication.swift:188-191`), so the store test exercises the same shape; an end-to-end drain-through-reconciler test does not exist in the suite I grepped. Classification: closed, integration coverage thinner than "real readback tests" suggests (test-reviewer's call).

### M3 (historical canary authority): CLOSED in source, one seam observation
- `verifyProviderLaunchAuthority` (HerdrPiWorkflowExecutor.swift:4912-4927): the `permit == nil` branch now unconditionally throws `effectIdentityMismatch`; the delta shows the 55-line historical fallback and `hasActiveHistoricalProviderAuthority` deleted.
- Fresh-execution deny at 4215-4219 (`if case .historicalCanary = RolloutEffectTaskContext.current?.mode { throw .effectAdmissionClosed }`) sits after settled-result replay (4198-4211) and before `runs.runForSlot` (4221). It sits AFTER `preparer.prepare(request)` (4196), but `PiJobWorkflowPreparer.prepare` (PiJobWorkflowPreparer.swift:47-85) is pure (digest checks, profile lookup, prompt JSON), so "denied before preparing a slot" holds for anything durable.
- Single seam? Grep of `historicalCanary`/`requireHistoricalCanary`: every store entry ends in `requireHistoricalCanary(...) -> Never` (Transactions.swift:638-641); the executor has exactly two consumers (4166 selection, 4217 deny). No branch assumes fresh historical authority any more. The retained canary state machinery (`activeCanaryPiFreshRetry` 4740-4750, Q4 rollover 4854/5006-5100, role-host replacement 5028/5086/5458, credential expectation 5145-5160) can only terminate at `enqueuePreparedLaunch`/`ensureCommandPublished` -> `verifyProviderLaunchAuthority(permit: nil)` -> throw (nil-permit call sites: 1805, 1807, 5660, 6189, 6629, 6859, 7036). See Minor 4.
- Coordinator routing: `JobCoordinator.runCanary` (JobCoordinator.swift:396-424) still leases-then-runs a `.prReview` workflow under `.historicalCanary`, but `PullRequestReviewJobWorkflow.run(jobID:)` (357-363) rebinds the task-local to `.workflow(jobID:)`, so the fresh canary actually executes as an ordinary workflow and is denied by `loadActiveBinding`/`effectAdmissionOpen`, not by the historical rule; `failJob` classifies `RolloutAuthorityError` as `.permanent` (JobCoordinator.swift:161-175) and the job lands `blocked`. `JobCoordinatorTests.swift:118-156` pins exactly this (`#expect(... state == .blocked)`). Fail-closed and tested, but the quarantine is implicit (generic failure), see Minor 1 and Minor 2.

### M4 (runtime fixes): CLOSED
- Second terminal transition removed: `rolloutStatus` (ProductionEngineJobRuntime.swift:468-486) now catches `previewDrift` without calling `markRecoveryRequired`, then pauses only if `latestStatus()?.authorization.state != .active`. `activeStatus` (RolloutAuthorityStore.swift:329-342) is the single writer of the `failed/CONFIGURATION_DRIFT` transition.
- Recovery approval vs authorization expiry: guard at 566-570 (`action != .readbackThenContinueExact || now < authorizationExpiresAtMilliseconds`) with the payload field `RolloutAuthority.swift:1588` populated at `:1648` and validated at `:1675,1693`. `resumeExactRecovery` also requires `expires_at_ms > now` (RolloutAuthorityStore.swift:600) so the store was already fail-closed; the runtime fix makes the refusal `staleEvidence` instead of `invalidStateTransition`.
- Composition seams: `EngineDispatchGate` lost `private` (line 83) and `dispatchGate`/`commandGate` are injectable ONLY on the internal `reloadComposition:` initializer (211-225); the public production initializer (170-200) still constructs its own gates (181-183). `ProductionEngineReloadComposition.schedulerSnapshot` defaults to `{ nil }` (92), which makes `timingSnapshot()` report "not busy" for a composition that does not supply a snapshot: harmless for the production path (`reloadComposition == nil` -> `components?.scheduler.snapshot()`), and only test compositions use the seam. No widening of production authority found.
- Five commands consistency (gating / errors / state):
  | Command | Gate | Error on gate | State effects |
  |---|---|---|---|
  | previewRollout | paused, excl==0, !checkpointing | busy | none |
  | activateRollout | same | busy | `paused=false` + scheduler unpause in runtime (415-416) |
  | rolloutStatus | none | n/a | pauses on non-active lane (480-484) |
  | stopAndDrainRollout | latest is active + id/sha | staleEvidence | durable drain, gates closed, `paused=true` (506-510); un-pause never |
  | previewRolloutRecovery | paused + id/sha | staleEvidence | none |
  | executeRolloutRecovery | paused (+ service `beginExclusiveOperation`) | busy / staleEvidence | durable resume; **un-pause performed by EngineService** (EngineService.swift:656-665), not by the runtime |
  Gating asymmetries: `stopAndDrainRollout` and `executeRolloutRecovery` do not check `exclusiveOperations == 0 / !checkpointing` (the latter cannot, because the service wraps it in an exclusive operation). Safe only because `EngineService.send` serializes commands (`commandInProgress`, EngineService.swift:379-384) and is the sole production caller. See Minor 5.

### M2 (usage ledger INSERT trigger): not in my focus
Trigger present at DatabaseSchema.swift:2715-2835 with the four-source UNION; left to the database reviewer.

## Findings

### CRITICAL
None.

### MAJOR
None supported. Candidates examined and dropped with the reason:
- "Exact-continue recovery leaves the runtime paused" (runtime returns from `resumeExactRecovery` without un-pausing, 616-621): dropped, EngineService un-pauses at 656-665 after the checkpoint. Kept as Minor 5 (placement inconsistency).
- "Optional `JobCoordinator.rolloutAuthority` is a permissive default" (JobCoordinator.swift:83, `admitted` returns `true` when `rollout == nil` at 1063, unscoped discovery at 305-315): dropped as Major because production passes a non-nil store (ProductionEngineJobRuntime.swift:1667-1680), and with a nil coordinator authority every effect still fails at the non-optional effect gates (`GitHubBroker` 409/469 `rolloutAuthorityRequired`, `GitTransport` 507/552, store `loadActiveBinding`) and at the `repository_leases`/`jobs` triggers. Kept as Minor 6.
- "Canary command leases and blocks a job it can never run" (`executeCanary` -> `admitCanary` leases a queued generation-0 job under paused=1 with no open lane (JobCanary.swift:1141-1165), then `runCanary` fails it permanently): dropped as Major because the outcome is fail-closed, durable, and pinned by `JobCoordinatorTests.swift:118-156`; decision 30 forbids rewriting history, not blocking a fresh canary. Kept as Minor 1.

### MINOR
1. **Sources/JidokaCodeCore/Jobs/JobCoordinator.swift:414-420, 445-451, 477-483, 499-501** Outer `.historicalCanary` bindings are inert for `runCanary`: `PullRequestReviewJobWorkflow.run(jobID:)` (PullRequestReviewJobWorkflow.swift:357-363) rebinds `.workflow(jobID:)`, so the fresh canary is denied by the ordinary binding rule, not by the historical rule the wrapper announces. The other three entry points rebind `.historicalCanary` themselves inside the workflow (401-402, 449-450, 490-492), so the coordinator wrappers are redundant there too. -> Delete the coordinator-side `withValue` wrappers, or make the workflow honor an existing task-local (`RolloutEffectTaskContext.current ?? .workflow(jobID:)`) and add a typed `JobCoordinatorInternalError.historicalCanaryQuarantined` thrown before the lease transition in `runCanary` so the job is not leased-then-blocked. | evidence: a recording test double for `RolloutEffectAuthorizing` that captures `RolloutEffectTaskContext.current?.mode` at its first `reserveGitHubRead`, driven through `JobCoordinator.runCanary`, records `.workflow(jobID:)` (red under the current claim of historical quarantine, green after either fix).
2. **Sources/JidokaCodeCore/Jobs/JobCoordinator.swift:91-95** Same inert-wrapper pattern for readback: `RolloutWorkflowReadbackReconciler` binds `.readback(jobID:intentID:)` and then calls `workflows.workflow(for:).run(jobID:)`, which rebinds `.workflow`; the real `.readback` binding is set at the reader boundary (`MutationReconciler.swift:593-596`, `GitPublication.swift:188-191`). The M1 drain semantics therefore hinge on those two inner sites, while the code and the plan text point at the coordinator. -> Remove the outer wrapper and add a one-line comment at both inner sites naming them as the sole producers of `.readback`, or invert the precedence as in Minor 1. | evidence: `grep -rn "mode: \.readback" Sources` returns exactly three producers; the coordinator one is followed by a `run(jobID:)` whose first statement is `withValue(.workflow)` in all four workflows (IssueTriage 331-332, IssueImplementation 86-87/147-148, PiIssueImplementationExecutors 148-149, PullRequestReview 358-359).
3. **Sources/JidokaCodeCore/State/RolloutEffectAuthorityStoreTransactions.swift:320-431 and 441-546** `reserveReadback`/`reserveGitReadback` carry no Swift-side lane/pause guard; the only enforcer of the lane clause is the SQL trigger, whose refusal surfaces as `SQLiteStoreError.statementFailed(code: 19)` (no mapping exists: `grep -rn "statementFailed\|SQLiteStoreError" Sources` outside `SQLiteStore.swift` is empty). At the operator boundary this collapses to `staleEvidence` (EngineService.swift:1283-1286), but in `JobCoordinator.failures[].errorType` and logs it reads as a storage failure, and a future Swift-side change cannot be red-green tested against a typed error. -> Add the same `authorization.state`/`settings.paused` predicate the trigger uses to the source-selection query (or a preceding `scalarInt` check) and throw `RolloutAuthorityError.readbackNotAllowed`; keep the trigger as backstop. | evidence: probe in a temp database: activate an exact lane, mark one effect `sendStarted`, then `UPDATE app_settings SET paused = 1` by raw SQL (unreachable in production, but isolates the layer) and call `reserveGitHubRead` under `.readback`: today it throws `SQLiteStoreError`, not `RolloutAuthorityError`.
4. **Sources/JidokaCodeCore/Pi/HerdrPiWorkflowExecutor.swift:335-339, 4740-4750, 5006-5100, 5145-5160, 5360, 5458** Canary retry / Q4 rollover / role-host replacement launch machinery is retained although every path now terminates in `verifyProviderLaunchAuthority(permit: nil)` -> `effectIdentityMismatch` (4924-4927). Reachable-but-can-only-throw code is a drift risk: a later edit that threads a permit into one of these paths silently re-opens fresh authority for a canary job. -> Either delete the launch halves of these paths (keep decode/replay of settled results per decision 30), or collapse them into one explicit early `throw RolloutAuthorityError.effectAdmissionClosed` at the top of `prepareFreshRetry`/Q4/replacement entry so the intent is one line, not seven nil-permit call sites. | evidence: call sites passing no permit at 1805, 1807, 5660, 6189, 6629, 6859, 7036; E5 states the rule "a Never return makes the branches terminal at compile time", which these Swift paths do not have.
5. **Sources/JidokaCodeCore/Application/ProductionEngineJobRuntime.swift:415-416 vs 616-621; EngineService.swift:656-665** Un-pause responsibility is split: `activateRollout` flips `paused=false` and un-pauses the scheduler inside the runtime, `executeRolloutRecovery` returns a durably active lane (`app_settings.paused = 0`, RolloutAuthorityStore.swift:661-671) while the runtime stays `paused = true` and relies on EngineService to un-pause. Any other caller of the public runtime method (tests, a future composition) leaves durable and in-memory pause disagreeing until restart, and restart re-marks the lane `recoveryRequired` (decision 21). Also `stopAndDrainRollout`/`executeRolloutRecovery` skip the `exclusiveOperations == 0 && !checkpointing` guard that `previewRollout`/`activateRollout` apply. -> Move the resume (`paused = false; setSchedulerPaused(false)`) into `executeRolloutRecovery` guarded by `report.authorization.state == .active`, mirroring `activateRollout`, and let EngineService only re-evaluate `setDispatchAllowed`. | evidence: source cited; probe: call `runtime.executeRolloutRecovery` directly with a continuable exact recovery, then `#expect(await runtime.timingSnapshot()?.paused == false)` is red today.
6. **Sources/JidokaCodeCore/Jobs/JobCoordinator.swift:83, 252-263, 281, 305-315, 1063** `rolloutAuthority: RolloutAuthorityStore?` keeps a nil arm that runs the pre-rollout unscoped flow (`jobs.recoverAtStartup`, unscoped discovery of every enabled repository, `admitted(_, by: nil) == true`). E6 removed exactly this shape from `DurableScheduler` ("removing the optionality removes the arm rather than guarding it"); the coordinator was left asymmetric. Production passes non-nil (ProductionEngineJobRuntime.swift:1667-1680) and the effect/lease gates still deny, so this is defence-in-depth debt, not a bypass. Four test constructions still use `rolloutAuthority: nil` (JobCoordinatorTests, PullRequestReviewJobWorkflowTests). -> Make the parameter non-optional (as E6 did) and give tests an explicit denying stub. | evidence: `grep -rn "rolloutAuthority: nil" Tests` = 4 hits; decision 4 and 14 text; E6 rationale.
7. **Sources/JidokaCodeCore/State/DatabaseSchema.swift:3405-3445 vs 3481-3518** The GitHub and Git readback insert triggers duplicate the lane/pause clause verbatim (11 lines). Already recorded as debt (round-6 Minor 9, E12 pattern); I add only that the E12 remedy (a byte-equality test that fails on drift) was applied to the lease pair and not to this pair. -> Apply the same drift test. | evidence: `grep -rn "bothTriggersShareTheSameGate\|readback_reservations_exact_started_effect" Tests` returns only `RolloutLeaseAuthorityTests.swift:392` (lease pair); no test references the readback trigger names.

### Summary
Overall structure: the authority model is now one-directional and fail-closed at every layer I traced (store typed guards, SQL triggers, non-optional effect authorities at broker/transport/store, scheduler admission with `?? .denied`). Swift and SQL agree on every reachable lane/pause state; the one disagreement cell is unreachable. The remaining structural debt is about explicitness, not authority: inert task-local wrappers in the coordinator that misdescribe where quarantine and readback are decided, retained canary launch machinery whose only outcome is a throw, and pause-state responsibility split between runtime and service.
Recommendation: ACCEPTABLE (zero Critical, zero Major). Minors 1, 2 and 5 are cheap and reduce the drift risk that a later round would otherwise turn into a Major.

## Could not verify
- M2 trigger semantics (database reviewer's scope).
- Any runtime behavior requiring `swift test` (all probes above are proposed, not run).
- Whether `HerdrPiWorkflowRuntime.execute` is ever called without a task-local context in production (the 4166 `.historicalCanary` selection); every production caller I found reaches it through a workflow that has already bound `.workflow`.
