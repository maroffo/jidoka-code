# ABOUTME: One-line tech-debt register; each entry points back to the plan that discovered it.
# ABOUTME: Items here were consciously deferred, not forgotten.

- Herdr socket path literal `.config/herdr/herdr.sock` duplicated in three targets (HerdrReadinessProbeCLI.swift:26, ApplicationEngineClient.swift:586, JidokaCodeEngineMain.swift:530); hoist to one JidokaCodeCore constant. Deferred from architecture review (Minor) during source-freeze of 2026-08-27_production-readiness-fast-path.md.
- Append-only/resume-denial SQL sweep in PiRunStoreTests.swift:2053-2072 asserts only SQLiteStoreError, not which trigger fired; match trigger RAISE messages to close the tautology risk. Deferred from test review (Minor) of 2026-08-27_production-readiness-fast-path.md; mitigated by trigger-name pinning in SQLiteStoreTests.swift:445-481.
- Preflight exit 69 (DB byte-guard trip) has no test seam; accepted as untestable without a write hook. Documented in test review of 2026-08-27_production-readiness-fast-path.md.
- Lease-gate arming clause and binding check are hand-duplicated across `repository_leases_rollout_authority_insert` and `_update` (DatabaseSchema.swift, 67 byte-identical lines); emit both from one helper as `appendOnlyTrigger(table:operation:)` already does. Deferred from architecture review (Minor) of 2026-09-03_progressive-production-automation.md, decision E12; drift is caught by `bothTriggersShareTheSameGate` and by the migration-10 digest pin.
- Coordinator-side `.historicalCanary` and `.readback` task-local wrappers in JobCoordinator.swift (91-95, 414-501) are inert: every workflow's `run(jobID:)` rebinds `.workflow`, and the real `.readback` producers are MutationReconciler.swift:593 and GitPublication.swift:188; a fresh canary is quarantined as a generic permanent failure after taking its lease rather than by a typed refusal before it. Deferred from round-7 architecture review (Minors 1-2) of 2026-09-03_progressive-production-automation.md; fail-closed and pinned by JobCoordinatorTests.
- `reserveReadback`/`reserveGitReadback` (RolloutEffectAuthorityStoreTransactions.swift:320-546) carry no Swift-side lane/pause guard, so the SQL trigger's refusal surfaces as `SQLiteStoreError.statementFailed(code: 19)` instead of a `RolloutAuthorityError`; the refused cell (`active, paused=1`) is unreachable in production. Deferred from round-7 architecture review (Minor 3).
- Canary retry, Q4 rollover and role-host replacement launch machinery is retained in HerdrPiWorkflowExecutor.swift although every path ends at `verifyProviderLaunchAuthority(permit: nil)` and throws; delete the launch halves or collapse them into one early typed refusal. Deferred from round-7 architecture review (Minor 4); decision 30 keeps settled-result replay.
- Un-pause responsibility is split: `ProductionEngineJobRuntime.activateRollout` un-pauses in the runtime while `executeRolloutRecovery` relies on EngineService.swift:656-665; stop and recovery also skip the exclusive/checkpointing guard that preview/activate apply, safe only because `EngineService.send` serializes. Deferred from round-7 architecture review (Minor 5).
- `JobCoordinator.rolloutAuthority` is still optional with a permissive nil arm (unscoped discovery, `admitted == true`), the shape E6 removed from DurableScheduler; production passes non-nil and the effect/lease gates deny regardless. Deferred from round-7 architecture review (Minor 6); four tests construct the coordinator with `rolloutAuthority: nil`.
- `HerdrPiWorkflowRuntime.execute` (HerdrPiWorkflowExecutor.swift:4159-4171) self-mints a `.workflow` task-local when none is installed; refuse instead, once the tests that call `execute` without a context install one. Deferred from round-7 security review (Minor); durable binding checks are unaffected.
- 49 of the 51 `roleHostReplacementRejectsEveryPostPreviewDrift` cases (HerdrPiWorkflowRuntimeTests.swift) hit the same admission guard at full fixture cost, with `expectedError` dead for those cases, and the 28-entry fault catalogue is referenced only by a count. Deferred from round-7 test review (Minor 3).
- Prior Minors 11/13/14 (redundant open-lane predicate on the `jobs_rollout_generation_*` triggers, subsumed scope-read UNIQUE, reservation index on `authorization_id, job_id, kind`) are optional DDL optimizations with no correctness defect (round-7 database review, EXPLAIN QUERY PLAN evidence); they still await Max's explicit scope decision because they change the migration-10 body and digest.
